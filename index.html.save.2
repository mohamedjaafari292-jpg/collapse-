<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warning</title>
    <style>
        body { background-color: black; color: red; text-align: center; font-family: 'Courier New', Courier, monospace; overflow: hidden; }
        .container { margin-top: 50px; }
        h1 { font-size: 3rem; text-shadow: 0 0 20px red; margin-bottom: 10px; }
        .skull { font-size: 150px; color: red; filter: drop-shadow(0 0 15px red); animation: pulse 1.5s infinite; }
        h2 { font-size: 1.8rem; margin-top: 20px; color: white; }
        h3 { font-size: 1.5rem; color: #888; margin-top: 30px; }
        @keyframes pulse {
            0% { transform: scale(1); opacity: 1; }
            5
